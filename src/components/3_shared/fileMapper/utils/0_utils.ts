import { getFilesPreview } from "./getFilesPreview";
import { proceedPreviewsHeaders } from "./proceedPreviewsHeaders";
import { isFileMapped } from "./isFileMapped";

const FileMapperUtils = {
  proceedPreviewsHeaders,
  getFilesPreview,
  isFileMapped,
};

export default FileMapperUtils;
