import { DefaultPageActionsMap } from "../types";

export const PAGE_ACTIONS: DefaultPageActionsMap = {
  CREATE: "create",
  EDIT: "edit",
  DELETE: "delete",
  SUBMIT: "submit",
  DOWNLOAD: "download",
};
